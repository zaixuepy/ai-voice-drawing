version: '3.8'

services:
  voicedraw:
    build: .
    ports:
      - '3000:3000'
    environment:
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
